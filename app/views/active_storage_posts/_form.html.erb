<%= form_with model: active_storage_post do |form| %>
  <div class="mb-3">
    <%= form.label :title, class: "uc-label" %>
    <%= form.text_field :title, class: "uc-input" %>
  </div>

  <div class="mb-3">
    <%= form.label :cover_image, "Cover image", class: "uc-label" %><br>
    <%= form.file_field :cover_image, class: "uc-file-input" %>
    <% if active_storage_post.cover_image.attached? %>
      <div class="mt-2"><%= active_storage_post.cover_image.filename.to_s %></div>
    <% end %>
  </div>

  <div class="mb-4">
    <%= form.label :attachments, class: "uc-label" %><br>
    <%= form.file_field :attachments, multiple: true, class: "uc-file-input" %>
    <% if active_storage_post.attachments.attached? %>
      <% active_storage_post.attachments.each do |attachment| %>
        <div class="mt-2"><%= attachment.filename.to_s %></div>
      <% end %>
    <% end %>
  </div>

  <div>
    <%= form.submit "Save", class: "uc-button uc-button-primary" %>
  </div>
<% end %>
