<h1>Edit comment</h1>

<div class="uc-card">
  <div class="uc-card-body">
    <%= render 'comments/form', path: comment_path(@comment), method: :patch, comment_content: @comment.content, comment_image: @comment.image, comment_attachments: @comment.attachments %>
  </div>
</div>
