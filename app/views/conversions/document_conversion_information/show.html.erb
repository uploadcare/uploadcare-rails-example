<h1>Conversion formats info</h1>

<div class="uc-card">
  <div class="uc-card-body">
    <%= form_tag(new_document_conversion_path, action: '', method: :get, class: 'p-1') do %>
      <div class="mb-3">
        <label class="uc-label" for="file">UUID: <%= params[:file] %></label>
      </div>
      <div class="mb-3">
        <label class="uc-label" for="file">Current Document Format: <%= @document_format %></label>
      </div>
      <% if @document_converted_groups.present? %>
        <div class="mb-3">
          <label class="uc-label" for="file">Converted Groups</label>
          <ul>
            <% @document_converted_groups.each do |group_format, group_uuid| %>
              <li><i><%= group_format %></i> - <%= link_to group_uuid, file_group_path(group_uuid) %></li>
            <% end %>
          </ul>
        </div>
      <% end %>
      <%= hidden_field_tag(:file, params[:file]) %>
      <div class="mb-3">
        <label class="uc-label" for="file">Possible Conversion Formats</label>
        <%= select_tag(:target_format, options_for_select(@document_conversion_formats), class: "uc-input") %>
      </div>
      <%= submit_tag "Add options and convert", class: "uc-button uc-button-primary", name: "" %>
    <% end %>
  </div>
</div>
