<h1>Upload a local file</h1>

<%= form_tag(upload_local_file_path, method: :post, class: 'uc-form-panel', multipart: true) do %>
  <div>
    <div class="upload-file">
      <label class="uc-label">File to upload</label>
      <input type="file" class="uc-file-input mb-3" id="uploadFileInput" name="file[file]">

      <div class="mb-3">
        <label class="uc-label" for="fileMimeType">Mime type</label>
        <input type="text" class="uc-input" id="fileMimeType" name="file[mime_type]">
      </div>

      <div class="mb-3">
        <label class="uc-label" for="filename">Filename</label>
        <input type="text" class="uc-input" id="filename" name="file[filename]">
      </div>
    </div>

    <div>
      <label class="uc-label" for="store">Store</label>
      <%= select_tag(:store, options_for_select(store_options), class: 'uc-input', name: 'file[store]') %>
    </div>
  </div>

  <%= submit_tag 'Upload file', class: 'uc-button uc-button-primary mt-4' %>
<% end %>

<script>
  (function() {
    const fileInput = document.getElementById('uploadFileInput');

    fileInput.addEventListener('change', evt => {
      let file = evt.currentTarget.files[0];
      if (!file) return;

      let filenameInput = document.getElementById('filename');
      filenameInput.value = file.name;

      let mimeTypeInput = document.getElementById('fileMimeType');
      mimeTypeInput.value = file.type;
    })
  })()
</script>
